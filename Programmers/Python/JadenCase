def solution(s):
    answer = ''
    tmp = s.split(" ")
    print(tmp)
    
    for i in tmp:
        if i == '':
            answer += " "
        else:
            answer += i[0].upper() + i[1: ].lower() + " "
            n = 0
    
    return answer[ : -1]
